## Cara Menggunakan Git

1. Ubah filenya dulu
2. `git add .` -> untuk menandai filenya masuk ke staged
3. `git commit -m "ini bisa diubah jadi commit message"` -> untuk commit file yang sudah masuk staged
4. `git push origin main` -> untuk push yang sudah di commit ke github
